{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "compile extension (repo root)",
      "type": "shell",
      "command": "pnpm compile && pnpm --filter ./webview build",
      "options": { "cwd": "${workspaceFolder}/.." },
      "group": "build",
      "presentation": {
        "reveal": "always",
        "panel": "shared",
        "focus": false
      },
      "problemMatcher": []
    }
  ]
}
