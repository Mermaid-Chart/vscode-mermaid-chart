import * as assert from 'assert';
import * as vscode from 'vscode';

suite('Preview Command Tests', () => {
    test('Should open preview', async () => {
        debugger;
        // Your preview test
        console.log('Testing preview...');
    });

    test('Should handle preview errors', async () => {
        // Your preview error test
    });
}); 